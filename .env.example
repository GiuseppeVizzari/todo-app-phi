# Auth0 Configuration for Local Development
# 
# For local development, create a .env.local file with these values:
# (The .env.local file is gitignored and won't be committed)
#
# For production (GitHub Pages), these values are stored as GitHub Secrets:
# - Go to repository Settings → Secrets and variables → Actions
# - Add VITE_AUTH0_DOMAIN and VITE_AUTH0_CLIENT_ID as secrets
#
# Get these values from: https://manage.auth0.com/dashboard

VITE_AUTH0_DOMAIN=your-domain.auth0.com
VITE_AUTH0_CLIENT_ID=your-client-id
